# API & web
fastapi
uvicorn[standard]
streamlit

# Distributed compute / ML
pyspark>=3.4.0

# Data & NLP
datasets
numpy
tiktoken
nltk
pandas

# Storage
pymongo

# HTTP
requests

# Tokenization / cost reporting
matplotlib

# Monitoring / logging
elasticsearch>=8.0.0,<9.0.0   # must match ES server version in docker-compose (8.x)
python-json-logger

# Environment management
python-dotenv

# Cloud — Azure Blob Storage (for Kaggle ↔ Azure data sync)
azure-storage-blob>=12.0.0

# Testing
pytest
